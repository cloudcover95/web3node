# JuniorHome owns the Metal/numpy tick slice

This tree is the HTML globe / mesh dashboards.

Additive tick code landed on `cloudcover95/JuniorHome`:
`web3node/svd_metal.py` (already mirrored conceptually) plus
`ternary_tda.py`, `svd_residual_tick.py`, `vietoris_rips.py`, `trit_cache.py`.

Do not copy Gemini `/opt/juniorcloud` Docker into this dashboard repo.
